

| MICROSERVICES ANTI-PATTERNS WORKSHOP Consumer Review Sheet Session 3  ·  Completed by the student whose service CALLS the reviewed service |
| :---- |

**What is a Consumer Review?**

A consumer review is done by the student who actually calls the reviewed service in the system — not a random peer. You have a personal stake: if the service you depend on is poorly designed, your service suffers. This review asks you to evaluate it from that dependency perspective.

| Your name |  Akawat Moradsatian | Your service |   Report Ingestion & Verification Service |
| :---- | :---- | :---- | :---- |
| **Service you are reviewing** |  MonitorDisaster Service | **Owner of that service** |  Tanat Kerdtip 1980 |
| **Which of your endpoints call their service?** | (None. They call me) |  |  |

**Session 3 Consumer Review Questions**

| Question — from your perspective as a consumer | Your finding — quote specific evidence from their contract |
| :---- | :---- |
| TIMEOUT \#8 If their service makes an outbound call with no timeout and it hangs, how long before your service also hangs? What is your worst-case exposure? | **ผมไม่ได้เรียก MonitorDisaster Service โดยตรง** - พวกเขาเรียกผม (POST /v1/reports) เพื่อส่งข้อมูล IoT sensor ดังนั้น ถ้าพวกเขา hang ไม่กระทบ service ผมโดยตรง แต่ **worst-case exposure คือ 30 seconds** (ตาม API Gateway timeout ของผม) ถ้า Lambda ของผมประมวลผล request จากพวกเขานาน ปัญหาสำคัญคือ MonitorDisaster Service ระบุ "Timeout: 30s" ใน contract แต่**ไม่ได้ระบุว่า external API calls** (กรมอุตุฯ, กรมชลประทาน) **มี timeout หรือไม่** ถ้า external API hang → Lambda ของพวกเขา hang → data outdated → ประชาชนไม่ได้รับการแจ้งเตือนภัย Evidence: "Timeout: 30s" (Proposal lines 168, 225, 299) แต่ไม่มี external API timeout spec     |
| TIMEOUT \#8 Find the specific endpoint or outbound call in their contract that has no timeout defined. What operation is it? What does it call? | **พบปัญหา:** API Contract #1 (POST /ingest) ใช้รับข้อมูลจาก "API ภายนอก" (ตาม Proposal line 13: "รับผิดชอบการรวบรวม ข้อมูลจาก API ภายนอกแบบ Real-time") แต่**ไม่ได้ระบุว่า external API call มี timeout เท่าไหร่** Proposal ระบุแค่ Lambda API timeout=30s แต่ไม่ได้บอกว่า outbound call ไปยัง กรมอุตุฯ/กรมชลประทาน มี timeout และ circuit breaker หรือไม่ ถ้า external API hang → Lambda อาจ hang จนหมด 30s → data ไม่ถูก ingest ทัน → เกิด is\_outdated=true → ระบบเตือนภัยล้มเหลว **ต้องเพิ่ม:** External API timeout config (แนะนำ 10s) และ fallback strategy (cache last known good data) Evidence: Proposal ไม่มีระบุ external API timeout spec     |
| STATIC CONTRACT \#9 If they renamed a field in their response tomorrow without telling you, how would you find out? Is there a notification mechanism in their contract? |  I wound’t find because there are not declare anyware in the api. Also if they rename it might got the 401 Bad request instead\!     |
| STATIC CONTRACT \#9 Does their contract document have a versioning policy? Is there a definition of what counts as a breaking change? What is missing? | **ไม่มี versioning policy.** Async message มี "Version: v1" แต่**ไม่มีเอกสารอธิบาย versioning strategy**: (1) **API paths ไม่มี version prefix** - ใช้ /api/monitor-disaster แทน /api/v1/monitor-disaster ทำให้ยากต่อการรัน multiple versions พร้อมกัน (2) **ไม่มีนิยาม breaking change** - ไม่ทราบว่า การเปลี่ยน impact\_level จาก 1-4 เป็น enum ["NORMAL", "WATCH", "WARNING", "CRITICAL"] ถือเป็น breaking หรือไม่ (3) **ไม่มี deprecation policy** - จะแจ้งล่วงหน้ากี่วัน จะ support backward compatibility นานแค่ไหน (4) **Event message ไม่มี schemaVersion field** ใน body (มีแค่ใน metadata) **ควรเพิ่ม:** docs/VERSIONING\_POLICY.md, เพิ่ม /v1/ ใน paths, เพิ่ม schemaVersion ใน message body Evidence: Proposal lines 100-106 (API paths), line 314 (Version metadata) แต่ไม่มี versioning policy doc     |
| OVERALL DEPENDABILITY Based on the full contract review — is this service safe to depend on at the current checkpoint? What is the single most important thing the owner needs to fix before you would integrate with confidence? | **Safe with conditions.** MonitorDisaster Service มี design ที่ดี: (1) ✅ Error responses มี structured format พร้อม traceId (ดีกว่า service ผม!) (2) ✅ มี Idempotency checks (timestamp validation) (3) ✅ มี is\_outdated flag สำหรับ data staleness detection (4) ✅ มี Manual Override capability แต่มี **3 critical gaps**: (1) 🔴 **External API timeout ไม่ระบุ** - ไม่ทราบว่ามี timeout config สำหรับ external API calls (กรมอุตุฯ/ชลประทาน) หรือไม่ อาจทำให้ Lambda hang (2) 🟡 **ไม่มี versioning policy** - breaking changes อาจเกิดโดยไม่มีการแจ้งเตือน (3) 🟡 **API paths ไม่มี /v1/** - ยากต่อการ versioning **สิ่งสำคัญที่สุด:** เพิ่ม **external API timeout configuration และ circuit breaker** ในเอกสาร (เช่น "External API timeout=10s, max retries=2, fallback=cached data + is\_outdated=true") เพราะนี่คือ disaster warning system - ถ้า hang → ไม่มีการแจ้งเตือน → อาจเกิดความสูญเสียชีวิต        |

**After the session — what will you ask the service owner to fix?**

Write a brief message you will send to the service owner after class. Be specific: name the anti-pattern, the exact location of the gap, and what you need fixed before you depend on this service.

| **เรียน Tanat (Service Owner - MonitorDisaster Service)** จากการทำ Consumer Review ในฐานะ Report Ingestion & Verification Service ที่จะส่งข้อมูล disaster reports ผ่าน EventBridge ให้คุณนำไปประมวลผล พบ gaps สำคัญ 3 ข้อที่ต้องแก้ไขก่อนจะ integrate ในระบบ disaster response ได้อย่างมั่นใจ: **1. Anti-Pattern #8 (Timeout) - CRITICAL:** Proposal ของคุณระบุว่า "รับข้อมูลจาก API ภายนอกแบบ Real-time" (line 13) และ API Contract #1 (POST /ingest) ใช้ดึงข้อมูลจากกรมอุตุฯ/กรมชลประทาน แต่**ไม่ได้ระบุว่า external API calls มี timeout configuration หรือไม่** ถ้า external API hang → Lambda ของคุณ hang → data ไม่ fresh → is\_outdated=true → ประชาชนไม่ได้รับการแจ้งเตือนภัย **กรุณาเพิ่มใน contract:** "External API timeout = 10s, max retries = 2, circuit breaker after 3 consecutive failures, fallback = return cached data + set is\_outdated=true" **2. Anti-Pattern #9 (Static Contract):** API paths ของคุณใช้ /api/monitor-disaster แต่**ไม่มี version prefix** (/v1/) และ**ไม่มี versioning policy document** ถ้าคุณเปลี่ยน impact\_level จาก 1-4 เป็น enum หรือ rename fields ใน DisasterStatusChanged event ผมจะรู้เมื่อ production fail เท่านั้น **กรุณา:** (1) เพิ่ม /v1/ ใน API paths (2) สร้าง docs/VERSIONING\_POLICY.md ระบุ breaking changes definition และ deprecation timeline (3) เพิ่ม "schemaVersion": "1.0" field ใน async message body (ไม่ใช่แค่ metadata) **3. Observability (Bonus):** Error responses ของคุณมี traceId แล้ว (ดีมาก!) แต่ควรเพิ่ม X-Trace-Id ใน response headers ด้วยเพื่อ distributed tracing ผมต้องการสิ่งเหล่านี้แก้ไขก่อนจะเริ่ม integration testing ในสัปดาห์หน้า เพราะนี่คือระบบเตือนภัยที่ต้องการ reliability สูงสุด ขอบคุณครับ — Akawat (Report Ingestion & Verification Service) |
| :---- |

