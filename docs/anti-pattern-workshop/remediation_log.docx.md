

| CS366 2/2568 \- MICROSERVICES ANTI-PATTERNS WORKSHOP Remediation Log Submit alongside your revised service document after all 3 sessions |
| :---- |

| Student name | Akawat Moradsatian | Student ID | 6609681231 |
| :---- | :---- | :---- | :---- |
| **Service name** | Report Ingestion & Verification Service |  |  |

**How to complete this log**

For each anti-pattern: (1) state whether you detected it in your service, (2) state whether you fixed it, (3) describe exactly how you fixed it OR explain why it could not be fixed now and what the proposed resolution is. Vague answers ('looks fine', 'not applicable') will not improve your design/implementation and receive partial marks. Every row requires specific evidence or a specific justification.

| Anti-Pattern | Detected? | Fixed? | How Fixed / Why Not Fixed / Proposed Resolution |
| :---- | ----- | ----- | :---- |
| **\#1 Distributed Monolith** Can you deploy without any other service running? | No | Yes | **Not Detected.** Service สามารถ deploy และ start ได้อย่างอิสระโดยไม่ต้องพึ่ง service อื่น มีการใช้ SQS เป็น buffer และมี graceful degradation สำหรับทุก dependency (ดูใน Dependency Mapping ในเอกสาร Proposal) หาก Incident Service หรือ Gemini API ล่ม ระบบยังทำงานได้ปกติ ไม่มี shared configuration files กับ service อื่น |
| **\#2 Shared Database** Does any other service have a connection string to your database? | No | Yes | **Not Detected.** มี DynamoDB ของตัวเอง 3 tables (Reports, AuditLogs, Stats) ไม่มี service อื่นเข้ามาอ่านหรือเขียน database ของเราโดยตรง การอ้างอิง linked\_incident\_id เป็น Reference-Only (Foreign Key แบบ Logical) ไม่ได้ไปแก้ไข database ของ Incident Service (หลักฐาน: terraform/dynamodb.tf และ Proposal หัวข้อ Linked Data) |
| **\#3 Chatty Services** Does your list endpoint return enough fields to avoid follow-up calls? | No | Yes | **Not Detected.** GET /v1/reports คืนข้อมูลครบถ้วนใน 1 call รวม reporter\_info, trust\_score, suggested\_category, geo\_location, timestamps และ status ไม่จำเป็นต้อง follow-up call (หลักฐาน: src/handlers/api\_handler.py:\_handle\_list\_reports และ API Contract ในเอกสาร) การส่ง Event แบบ Fan-out ใช้ Rich Payload มีข้อมูลครบ ไม่ต้องให้ downstream services ย้อนกลับมาเรียก API เรา |
| **\#4 Over-Microservices** Does your service have genuine standalone domain value? | No | Yes | **Not Detected.** Service มี standalone value ชัดเจน: รับรายงานภัยพิบัติจากหลายแหล่ง → กรอง spam/duplicate → AI scoring → human verification สามารถอธิบายได้ในประโยคเดียวโดยไม่ต้องอ้างถึง service อื่น มี bounded context ชัดเจนในการทำ "Verification" (หลักฐาน: Proposal หัวข้อ Service Purpose และ Service Boundary) |
| **\#5 God Service** Can you describe your service in one sentence? Is the out-of-scope section specific? | No | Yes | **Not Detected.** Service อธิบายได้ว่า "รับและตรวจสอบความน่าเชื่อถือของรายงานภัยพิบัติก่อนส่งต่อให้ระบบอื่น" มี Out-of-scope section เขียนไว้ชัดเจนใน Proposal (ไม่ทำ web scraping, ไม่สร้าง official incident, ไม่บริหารทรัพยากรกู้ภัย) ไม่ก้าวก่ายหน้าที่ service อื่น (หลักฐาน: Proposal หัวข้อ 5 Service Boundary) |
| **\#6 Tight Coupling** Is a circuit breaker or fallback defined for every synchronous outbound call? | No | Yes | **Not Detected.** มี synchronous dependency เพียง 1 ตัว คือ Google Gemini API และมี fallback ชัดเจน: (1) Multi-key rotation กรณี 429 rate limit (2) Model fallback chain (flash-lite → flash → 3.1-flash-lite) (3) หาก fail ทั้งหมด จะตั้ง trust\_score=50 และ flag ai\_analysis\_failed=true แล้วส่งเข้า manual review queue (หลักฐาน: src/services/gemini\_service.py และ src/handlers/ingestion\_worker.py) ไม่มี synchronous call ไปยัง Incident Service (ใช้ EventBridge แทน) |
| **\#7 Observability** Is traceId in every response? Are logs structured JSON with operation context? | Yes | Partial | **Detected.** มี structured JSON logging พร้อม request\_id ใน src/utils/logger.py แต่ **ยังไม่มี X-Trace-Id header ใน API responses** และ **error responses ไม่มี traceId field** (ตรวจสอบใน src/utils/response.py เห็นว่า error response มีแค่ error, message, detail) **Proposed Resolution:** (1) เพิ่ม X-Trace-Id header ในทุก response โดยแก้ไข response.py ให้เพิ่ม "X-Trace-Id": request\_id เข้าไปใน headers (2) เพิ่ม "trace\_id" field ใน error response body (3) Propagate trace\_id ผ่าน EventBridge events |
| **\#8 Timeout (Richards)** Does every outbound call have an explicit timeout value and a defined fallback? | No | Yes | **Not Detected.** ทุก outbound call มี explicit timeout: (1) Gemini API: GEMINI\_TIMEOUT=15 seconds พร้อม fallback (src/config.py:35 และ gemini\_service.py) (2) Lambda API Handler: 30 seconds timeout (terraform/variables.tf:51-54 และ lambda.tf:80) (3) Lambda Ingestion Worker: 60 seconds (variables.tf:56-60) (4) SQS visibility: 90 seconds (variables.tf:82-86) (5) API Gateway: ใช้ default 30 seconds (AWS default) มี fallback ครบทุก call: Gemini fail → trust\_score=50, Lambda timeout → return 500 error, SQS timeout → retry ตาม maxReceiveCount=3 แล้วส่ง DLQ |
| **\#9 Static Contract (Richards)** Is there a versioning policy beyond just /v1/ in the URL? | Yes | Partial | **Detected.** ใช้ /v1/ ใน API endpoints ทั้งหมดแล้ว (terraform/api\_gateway.tf:21-24) และ EventBridge events มี schemaVersion field (event\_publisher.py) แต่ **ยังไม่มี written versioning policy** ที่อธิบายว่า breaking change คืออะไรและจะจัดการอย่างไร **Proposed Resolution:** สร้างเอกสาร docs/VERSIONING\_POLICY.md ที่ระบุ (1) Breaking changes คืออะไร (เช่น ลบ field, เปลี่ยนชนิด, เปลี่ยน enum values) (2) Non-breaking changes คืออะไร (เพิ่ม optional field, เพิ่ม enum value) (3) กระบวนการ deprecation (ประกาศล่วงหน้า 30 วันก่อน remove endpoint, ใช้ X-Deprecated-Version header) (4) Event schema evolution strategy (เพิ่ม schemaVersion ทุกครั้งที่มี breaking change) |

**Reflection Questions**

| Which anti-pattern was hardest to fix, and why? What would need to change in your design to resolve it properly? **Anti-Pattern #7 (Observability)** เป็นเรื่องที่ยากที่สุดในการแก้ไข เพราะแม้ว่าเราจะมี structured JSON logging อยู่แล้ว แต่การ propagate traceId ผ่านทั้ง API responses, error messages และ EventBridge events ต้องแก้ไขหลายจุด: (1) response.py ต้องรับ request\_id เป็น parameter และเพิ่ม X-Trace-Id header (2) ทุก handler ต้องส่ง request\_id ไปให้ response builders (3) EventBridge events ต้องมี trace\_id field (4) Error responses ต้อง restructure ให้มี trace\_id ซึ่งอาจทำให้ clients ที่ existing ต้องปรับตัว การแก้ให้ถูกต้องต้องทำ backward-compatible โดยอาจต้องรองรับทั้ง format เก่าและใหม่ไปพร้อมกันชั่วคราว |
| :---- |
| **Did the peer or consumer review reveal anything you had missed in your self-review? What was it?** ใช่ครับ จาก Peer Review ทำให้ผมตระหนักว่า error response format ของผมยังไม่เป็น standard ที่ดีพอ (ไม่มี error\_code และ trace\_id) ซึ่งเวลา debug ใน production จะลำบาก และจาก Consumer Review (MonitorDisaster Service ที่เรียกเรา) ทำให้รู้ว่าเราควรมี explicit timeout documentation ใน API contract ชัดเจนกว่านี้ เพื่อให้ downstream services วางแผน circuit breaker timeout ได้ถูกต้อง ตอนนี้เราบอกแค่ "default API Gateway 30s" แต่ไม่ได้บอกว่า worst-case latency คือเท่าไหร่ (เช่น Gemini retry อาจใช้เวลานานกว่า) |
| **If you were building this service from scratch knowing what you know now, what would you design differently?** หากทำใหม่ ผมจะ: (1) **Design for Observability from Day 1** — เริ่มต้นด้วย trace\_id และ correlation\_id ในทุก request/response/event ตั้งแต่ต้น ไม่ใช่มาเพิ่มทีหลัง (2) **Explicit Contract Versioning** — สร้างเอกสาร VERSIONING\_POLICY.md พร้อมกับ API Contract แรก รวมถึง deprecation timeline และ breaking change checklist (3) **Timeout Budget Planning** — คำนวณ timeout ทั้งระบบให้ชัดเจน (Gemini 15s + processing 5s + buffer 10s = API timeout 30s) และระบุไว้ใน contract (4) **OpenAPI Specification** — ใช้ OpenAPI 3.x เขียน API contract เป็น machine-readable spec เพื่อ auto-generate docs และ client SDKs (5) **Health Check with Dependencies** — ปรับปรุง /health endpoint ให้บอกสถานะของแต่ละ dependency (Gemini, DynamoDB, SQS) เพื่อ debugging ง่ายขึ้น |

**Grading reminder**  Tier 1 (must pass): \#1 deploy independently, \#2 no shared DB, \#8 timeouts defined.  Tier 2 (standard): \#3 composite endpoints, \#6 fallbacks defined, \#7 traceId propagated.  Tier 3 (exceeds): \#9 versioning policy, reflection quality.